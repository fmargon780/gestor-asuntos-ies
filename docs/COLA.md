# Cola de instrucciones para Claude Code

Aquí se apuntan, en orden, las instrucciones pendientes. Cada una es un documento de `docs/`.
Francisco lanza siempre la misma línea; Claude Code hace lo que esté pendiente, de arriba abajo.

## Reglas para Claude Code

1. Lee antes `docs/CONTEXTO.md`.
2. Coge la primera instrucción con estado **PENDIENTE**. Cámbiala a **EN CURSO** con la fecha y
   sube ese cambio en el primer commit del trabajo. Así, si otra sesión abre esta cola, sabe que
   ya hay alguien con ella y no la repite.
3. Antes de empezar una instrucción, comprueba si ya está hecha por otro camino (mira si existen
   los ficheros o funciones que pide). Si ya está hecha, márcala **HECHA** con una nota y pasa a
   la siguiente.
4. Al terminar una, márcala **HECHA** con la fecha y la versión publicada, y sigue con la
   siguiente PENDIENTE. No pares hasta que no quede ninguna. **La hora de `App.VERSION` sale del
   reloj de verdad** (`TZ='Europe/Madrid' date`, receta exacta en `js/version.js`), nunca a ojo:
   el 17-sep-2026 salieron versiones con horas por delante de la real.
5. Si una instrucción no puede completarse, márcala **BLOQUEADA** con el motivo en una línea y
   sigue con la siguiente. Nunca dejes el repositorio con las pruebas en rojo.
6. Si encuentras una instrucción **EN CURSO** de otra sesión y no eres tú quien la empezó,
   sáltala y coge la siguiente PENDIENTE.
7. No preguntes nada a Francisco. Al final, un mensaje corto: qué instrucciones has hecho, la
   versión publicada, y qué va a ver distinto en pantalla.
8. Al terminar cualquier instrucción: actualiza `docs/CONTEXTO-CORTO.md` y `docs/CONTEXTO.md`
   **sustituyendo la línea vieja, no añadiendo una debajo**. Si algo deja de ser verdad, se borra.
9. Añade a `docs/HISTORIA.md` lo que merezca recordarse, con su fecha. No dejes que
   `docs/CONTEXTO-CORTO.md` pase de 160 líneas.
10. **Antes de subir nada, vuelve a bajar `main`.** Marcar la fila EN CURSO no basta: otra sesión
    puede haber fusionado su trabajo mientras tanto, y subir ficheros enteros sin releer pisa lo
    suyo. Pasó el 16-sep-2026 con las filas 13 y 14, el 17-sep-2026 con la fila 38, y otra vez el
    17-sep-2026 con `vercel.json` en la fila 48 (ver la nota de esa fila y `docs/HISTORIA.md`).
    **Vuelve a bajar `main` justo antes de cada llamada que suba un fichero, no una sola vez al
    empezar el cierre.**
11. **Nunca subas un fichero con un texto de relleno en vez de su contenido.** Si no tienes el
    contenido entero delante, no lo subas: bájalo antes. El 17-sep-2026 `docs/CONTEXTO.md` se
    quedó en `main` con la palabra `PLACEHOLDER_WILL_REPLACE` y nada más, y hubo que recuperarlo
    del historial de git. Después de subir, vuelve a bajar lo subido y compruébalo.
12. **Algunas sesiones no pueden subir un fichero de más de unos 45-50 KB de una sola vez**:
    la llamada que sube el contenido se corta sola sin avisar de ningún error, y el fichero queda
    en `main` con solo el primer trozo. Pasó el 17-sep-2026 con `docs/HISTORIA.md` (ver la nota al
    final de esta cola). **Antes de subir un fichero grande** (`docs/CONTEXTO.md`,
    `docs/HISTORIA.md`), compruébalo después de subirlo (`get_file_contents` o
    `git show origin/main:<ruta>`) y compara el tamaño con el de antes de escribirlo: si ha
    quedado más corto de lo esperado, esa sesión no puede con este fichero de una vez, y hay que
    dejarlo apuntado aquí en vez de reintentarlo mil veces.
13. **Como máximo dos subidas por fila.** Cada push que llega a GitHub le cuesta una publicación
    a Vercel, y el plan gratuito solo da 100 al día: el 17-sep-2026 se agotaron y la web se quedó
    sin actualizar hasta el día siguiente. Una subida para marcar la fila **EN CURSO** (regla 2) y
    una sola al terminar, con el código, las pruebas, `docs/COLA.md`, `docs/CONTEXTO-CORTO.md`,
    `docs/CONTEXTO.md` y `docs/HISTORIA.md` en el mismo commit. Nada de un commit por fichero, ni
    de "completa el commit anterior": se prepara todo y se sube una vez. Ver
    `docs/NO-GASTAR-PUBLICACIONES.md`.
14. **Nunca uses `$(cat fichero)` ni ninguna sustitución de shell como valor de `content` al
    subir un fichero: el servidor no lo ejecuta, lo sube tal cual, como texto literal.** El
    17-sep-2026 esto dejó `docs/COLA.md` en 35 bytes con el comando sin ejecutar. El contenido
    tiene que ir escrito entero, de verdad, en el propio parámetro.

## Reglas para Francisco

- Mientras Claude Code está trabajando, **no se lanza otra vez**. Las instrucciones nuevas se
  apuntan aquí y esperan.
- Cuando Claude Code termina, se vuelve a pegar la misma línea. Si no queda nada pendiente,
  Claude Code lo dice y no toca nada.

## La línea para lanzar

    Lee docs/CONTEXTO.md y después docs/COLA.md. Haz en orden todo lo que esté PENDIENTE, siguiendo las reglas de la cola, sin preguntarme nada. Al terminar, dime en pocas frases qué has hecho, qué versión está publicada y qué voy a ver distinto en pantalla.

## La cola

| Nº | Instrucción | Estado | Notas |
|---|---|---|---|
| 1 | `docs/PLAN-ROBUSTEZ-2026-09.md` | HECHA | Copias de seguridad, conflictos de Dropbox, pruebas automáticas, fichas sin carpeta y nombres repetidos. Versión publicada `11-sep-2026 · 05:33`. Detalle en `docs/HISTORIA.md` |
| 2 | `docs/REGISTRO-EN-UN-PASO.md` | HECHA | Botón Registrar sin nombrar dos veces, con lectura sola del sello de Séneca. Comprobado 11-sep-2026. Detalle en `docs/HISTORIA.md` |
| 3 | `docs/CAMPOS-POR-TIPO.md` | HECHA | Campos propios por tipo de asunto, configurables en Ajustes. Versión publicada `11-sep-2026 · 12:00`. Detalle en `docs/HISTORIA.md` |
| 4 | `docs/TERCEROS-RELACIONADOS.md` | HECHA | Personas y entidades relacionadas con un asunto, con nota (no copia) al archivar. Versión publicada `11-sep-2026 · 12:00`. Detalle en `docs/HISTORIA.md` |
| 5 | `docs/NO-DUPLICAR-ASUNTOS.md` | HECHA | Parada al crear un asunto duplicado, y unir los que ya existían. Versión publicada `11-sep-2026 · 13:08`. Detalle en `docs/HISTORIA.md` |
| 6 | `docs/AJUSTES-AGIL.md` | HECHA | Pestañas, buscador cruzado y aviso en vivo en Ajustes; barra lateral fija. Versión publicada `11-sep-2026 · 14:58`. Detalle en `docs/HISTORIA.md` |
| 7 | `docs/PAPELERA.md` | HECHA | Papelera compartida: nada se borra del todo a la primera. Versión publicada `11-sep-2026 · 16:20`. Detalle en `docs/HISTORIA.md` |
| 8 | `docs/UNIR-VER-DENTRO.md` | HECHA | Pantalla propia "Duplicados", con "No son el mismo". Versión publicada `11-sep-2026 · 17:15`. Detalle en `docs/HISTORIA.md` |
| 9 | `docs/REPARTO-CONTEXTO.md` | HECHA | Repartido `CONTEXTO.md` en `CONTEXTO-CORTO.md`, `CONTEXTO.md` y `HISTORIA.md`, y podada esta cola; no toca código ni publica nada nuevo. |
| 10 | `docs/ARREGLOS-USO-2026-09-14.md` | HECHA | El 3 (borrar en Por clasificar) ya estaba hecho, de la papelera (fila 7). Escape y salida en toda pantalla, copiar el nombre de un relacionado en orden normal, y las carpetas temporales de Drive/Dropbox fuera de Asuntos abiertos. Subido a `main` (versión `14-sep-2026 · 16:36`); esta sesión no ha podido comprobarlo con `curl` (su red no llega a la web publicada) — falta confirmarlo en el navegador. Detalle en `docs/HISTORIA.md`. |
| 11 | `docs/CORREOS-AL-ASUNTO.md` | HECHA | Terminada 16-sep-2026, en otra sesión en paralelo. Botón "Elegir asunto" en cada correo de la bandeja, con "Podrían encajar" y la lista completa; huella del hilo (`hilos`) en `asuntos.json`, que manda sobre la adivinación por texto; `seguidos.json` para que el recolector de Apps Script siga los hilos ya enganchados y devuelva a la bandeja las respuestas y los correos enviados. Las 19 pruebas en verde. Versión publicada y comprobada con `curl`: `16-sep-2026 · 20:41`. **Falta que Francisco pegue el script nuevo en `script.google.com`** (las tres primeras líneas de `apps-script/gestor-correos.gs` dicen cómo): sin eso, las respuestas no vuelven solas. Detalle en `docs/HISTORIA.md`. |
| 12 | `docs/DOCUMENTO-A-ASUNTO-EXISTENTE.md` | HECHA | Terminada 16-sep-2026, en otra sesión en paralelo (con mejor diseño que el de esta: un solo cuadro `js/elegir-asunto.js` compartido desde el principio con la bandeja de correos, en vez de dos por separado). Botón "Meter en un asunto" en cada documento de Por clasificar, entre "Crear asunto con él" y "Borrar": lleva el papel a la carpeta de un asunto que ya existe y abre el cuadro de ponerle nombre. Esta sesión había hecho su propia versión, en paralelo y sin saber que la otra ya la tenía resuelta; al fusionar se ha descartado la de aquí y se ha dejado la que ya estaba en `main`, que es la que unifica los dos elegidores. Si ya hay un fichero con ese nombre no se pisa, y si el traslado falla el documento sigue en Por clasificar. Versión publicada y comprobada con `curl`: `16-sep-2026 · 21:40`. Detalle en `docs/HISTORIA.md`. |
| 13 | `docs/ADJUNTAR-DOCUMENTOS-AL-CORREO.md` | HECHA | Bloque "Documentos de este asunto" en el cuadro de Correo, con casillas y límite de 20 MB; el script de Apps Script monta un borrador en Gmail con los adjuntos, respondiendo dentro del hilo si lo hay. Tarjeta "Borrador en camino" en Asuntos abiertos, con vigilancia cada 15 segundos. Batería completa en verde. **Aviso resuelto 16-sep-2026 · 22:40:** otra sesión marcó esta fila EN CURSO a las 20:04, hizo su propia versión sin saber que esta ya estaba fusionada, y al subirla pisó ficheros de las filas 13 y 14 (entre ellos la línea de `js/plantillas.js` de `index.html`). Se ha deshecho: `main` está otra vez byte a byte como antes de aquello, comprobado con `curl` contra lo publicado, y la batería completa vuelve a pasar. De ahí sale la regla 10 de esta cola. Versión publicada: `16-sep-2026 · 21:48`. Detalle en `docs/HISTORIA.md`. |
| 14 | `docs/PLANTILLAS-DE-CORREO.md` | HECHA | Plantillas por tipo de asunto, con huecos que se rellenan solos, para el correo y para el mensaje de Séneca; desplegable en los dos cuadros, aviso de huecos sin datos, recorte a 4.000 letras en Séneca. Bloque propio en Ajustes, con alta, edición, borrado y vista previa; firma y centro editables, ya no escritos a mano en el código. **Se había marcado HECHA sin estarlo del todo** (el bloque de Ajustes se hizo, pero `js/correo.js` no llegó a usar las plantillas): lo terminó de verdad la misma sesión de la fila 13, en el mismo pull request, con el desplegable en `#correo-comunes` que pedía el encargo. **En paralelo, esta sesión (la de las filas 15-17) hizo su propia versión del mismo arreglo, sin saber que ya estaba resuelto en `main`**; al fusionar, se ha mantenido la de `main` (llegó antes) y se ha descartado la de aquí, dejando solo el cambio que de verdad hacía falta encima: `js/correo.js` ahora llama a `Plantillas.valoresDeAsunto` (fila 17) en vez de tener su propia función de valores. Batería completa en verde. Detalle en `docs/HISTORIA.md`. |
| 15 | `docs/HITOS.md` | HECHA | Hitos dentro de cada asunto: los pasos de la guía pasan a ser hitos vivos, con estado, fecha, responsable, notas y documentos apuntados. Fichero nuevo `_GESTOR/hitos.json` (los compartidos pasan de once a doce); al archivar, el historial se escribe en `HISTORIAL DE TRAMITACION.txt`. Bifurcaciones como hito de decisión. Responsables configurables más papeles automáticos. Plazos en días hábiles. Las 10 pruebas de `pruebas/hitos.mjs` en verde. **Subido a la rama `claude/pending-queue-tasks-leziab` con pull request, no a `main`**: esta sesión no tiene permiso para publicar directamente (ver la nota al final de la cola). Versión `App.VERSION`: `16-sep-2026 · 22:18`, pendiente de fusionar el PR para que Vercel la publique. |
| 16 | `docs/QUE-ME-TOCA.md` | HECHA | Pantalla nueva "Qué me toca" que cruza todos los asuntos abiertos: "En tu tejado", "Esperando a otros" (con días parados) y "Sin fecha", con filtro por responsable, entrada en la barra con la cuenta de vencidos, y "abrir la ficha con el hito desplegado". Las 7 pruebas de `pruebas/que-me-toca.mjs` en verde. Subido con pull request a `claude/pending-queue-tasks-leziab`, no a `main` (ver la nota al final de esta cola). |
| 17 | `docs/PLANTILLAS-DE-DOCUMENTO.md` | HECHA | El gemelo en papel de la fila 14. Plantillas de Word colgadas de cada tipo de asunto, guardadas en `_GESTOR/PLANTILLAS`; botón "Generar documento" en la ficha del asunto, que saca una copia del Word con los huecos rellenos, ya guardada en la carpeta del asunto y con el nombre que mandan las reglas, sin preguntar nada. Módulos nuevos `js/docx.js` (ZIP y XML a mano, sin librerías, con reparación de huecos partidos entre `<w:t>`) y `js/plantillas-documento.js`; el bloque de Ajustes de plantillas de correo se sacó a `js/plantillas-ajustes.js` para no engordar `js/plantillas.js`. Reutiliza el motor de huecos de la fila 14: misma sintaxis de una llave, `Plantillas.rellenar`, mismo `plantillas.json` (clave nueva `documentos`, más `localidad`/`direccion`/`codigo`/`cargo`). Catálogo de huecos ampliado; `valoresDeAsunto` (antes privada de `js/correo.js`, con otro nombre en la versión de `main`) ahora es pública y async en `js/plantillas.js`, y es lo único de la fila 14 que ha tenido que tocarse otra vez al fusionar (ver esa fila). No crea ningún fichero compartido nuevo. Las 8 pruebas de `pruebas/plantillas-documento.mjs` (con jsdom, sin navegador) en verde; verificado además abriendo un `.docx` de salida con `unzip -t`/`zipinfo -v`. Subido con pull request a `claude/pending-queue-tasks-leziab`, no a `main` (ver la nota al final de esta cola). Acordado con Francisco el 16-sep-2026. |
| 18 | `docs/CORREO-EN-DOS-BUZONES.md` | HECHA | Matrícula del mensaje (`Message-ID`, igual en todos los buzones) guardada en `hilos` de `asuntos.json` (`matriculas`, `metidoPor`, `metidoEl`, los tres opcionales); `asuntoDeEsteCorreo` mira hilo propio, hilo de la respuesta, matrícula y solo entonces el texto. Línea gris "Ya está en el asunto «...» · lo metió Juan..." cuando el correo lo metió el otro buzón, con "Abrir el asunto" y "Quitar de mi bandeja" (sin papelera). El recolector (`apps-script/gestor-correos.gs`) sigue los hilos también por matrícula, con la cuenta de mensajes vistos guardada por buzón en `PropertiesService` (no comparada a ciegas con el `visto` compartido). De paso, "Último correo recogido" en Ajustes, tras descubrirse que el recolector llevaba 6 días escribiendo en una `GESTOR-BANDEJA` distinta de la que leía la aplicación. Prueba nueva `pruebas/correo-dos-buzones.mjs`; `pruebas/correos.mjs` ajustada a los campos nuevos. Batería completa en verde. **Fusionado con pull request. Falta que Francisco vuelva a pegar el script en `script.google.com`** (proyecto "Gestor - Correos"): sin eso las matrículas no se guardan y la mitad de esto no funciona. Detalle en `docs/HISTORIA.md`. |
| 19 | `docs/CSV-DEL-DOCUMENTO.md` | HECHA | `js/verificacion.js` nuevo, reutilizando `RegistroLector.textoDePrimeraPagina` (sacada de `js/registro-lector.js`). Busca la etiqueta del código (CSV, CVE, "Código de verificación"...) sin mayúsculas ni tildes, y una dirección `http(s)://` con `verifica`/`csv`/`cve`/`valida`/`cotejo`/`sede`. En `js/lector.js` (el panel de leer un correo): al abrir un PDF salen "Copiar el código" y "Abrir la verificación" si aparecen, sin retrasar el panel. No toca `js/visor.js`. Nada se guarda en ningún fichero compartido. Sin descargas automáticas: se descartó traer la copia auténtica sola, porque esas páginas piden formulario y cada administración tiene la suya. Prueba nueva `pruebas/verificacion.mjs`, sin PDF ni navegador. Batería completa en verde. No toca el script de Apps Script. Detalle en `docs/HISTORIA.md`. |
| 20 | `docs/REGISTRO-SIN-DUPLICAR.md` | HECHA | Acordado con Francisco el 17-sep-2026; es el punto 9 de `docs/PROXIMOS-ASUNTOS.md`. Se da la vuelta al orden: al abrir un asunto, `js/registro-sellado.js` mira los PDF de la carpeta con nombre que no ha puesto la aplicación (`Documentos.pareceDeLaAplicacion`), les lee el sello y guarda en `js/almacen.js` (de este ordenador, no compartido) si tenían sello o no, para no releerlos. Si hay sello sin resolver, sale arriba en la ficha un aviso ámbar con un desplegable de los documentos del asunto y el botón "No es un registro". Al elegir uno: el PDF sellado **se renombra** (`Carpetas.renombrarFichero`) con el nombre que hoy generaría el botón Registrar, el documento viejo se manda a la papelera (`Papelera.mandarDocumentoDeAsunto`) y se apunta la nota del registro con `Notas.sustituir` (nueva, en `js/notas.js`: sustituye la nota anterior del mismo documento en vez de duplicarla; también la usa ya el botón Registrar de siempre, que se deja igual para el papel que está en otro sitio). Si el nombre nuevo ya existe, se avisa y no se toca nada. `js/registro-lector.js` ahora lee hasta 10 páginas (paraba en la 1ª) y busca el sello con el texto normalizado y, si no aparece, sin ningún espacio: es lo que hacía que se detectara unas veces sí y otras no. `Carpetas.elegirFichero()` admite ya una carpeta de inicio (`startIn`); la usan `js/documentos.js` y `js/registro.js` (la del asunto abierto) y `js/traer-datos.js` (la de datos). Prueba nueva sin navegador `pruebas/registro-sin-duplicar.mjs` (normalización, sin sello, nombre ya puesto, ya mirado, nombre igual al de Registrar, colisión) y prueba en navegador `pruebas/registro-sellado.mjs` (detecta solo, asocia y renombra, "No es un registro" no vuelve a preguntar). Batería completa en verde. No toca el script de Apps Script. **Subido con pull request** (fmargon780/gestor-asuntos-ies#14), pendiente de que la batería en GitHub Actions se ponga en verde para fusionarlo solo (permiso permanente de Francisco); versión `App.VERSION`: `17-sep-2026 · 06:45`, se publicará al fusionar. **Francisco probará esto con papeles de verdad y avisará si hay que ajustar algo: es la única fila de esta cola donde su prueba hace falta de verdad.** |
| 21 | `docs/GRUPOS-DE-PERSONAS.md` | HECHA | Acordado con Francisco el 17-sep-2026; es el punto 3 de `docs/PROXIMOS-ASUNTOS.md`. `App.pintarBuscadorDeTercero` (js/asuntos-nuevo.js) gana un modo `multiple` opcional (casillas, barra fija con la cuenta, "Añadir los N señalados", cada señalado con su × para quitarlo sin perder el resto al cambiar de búsqueda o de categoría); el modo de siempre no cambia. `Nombres.nivelYEnsenanza(unidad)` nueva, reutilizando el mismo análisis de texto que `grupoCompacto`. En Relacionados (js/relacionados.js), "+ Añadir varios" abre ese buscador con, encima, los atajos de alumnado (unidad/nivel/enseñanza, solo matriculados de este curso) y "Meter un grupo entero"; `combinarRelacionados` añade sin duplicar al que ya estaba ni al propio tercero, sin preguntar uno a uno. Grupos propios en `js/grupos.js` (decimotercer fichero compartido, `_GESTOR/grupos.json`), con su bloque en Ajustes (crear, cambiar el nombre, ver/cambiar miembros con el mismo buscador, borrar por papelera); un miembro que ya no está en las listas se conserva, marcado aparte, hasta que se quita a mano con su ×. En Correo (js/correo.js), "Añadir un grupo" mete sus correos en **Copia oculta** (nunca en Para, por protección de datos), con aviso de quien no tiene correo; si el borrador se prepara con documentos (js/correo-adjuntos.js), el campo `cco` viaja en el `.envio.json` y `apps-script/gestor-correos.gs` lo pasa como `bcc`, poniendo la propia cuenta en "Para" si no hay nadie ahí. Fuera de esta fila, a falta de datos: departamentos, tutorías y equipos educativos. Pruebas nuevas `pruebas/grupos.mjs` (sin navegador) y `pruebas/grupos-navegador.mjs` (navegador de verdad); no se ha tocado ningún fichero de la fila 20 salvo para reutilizar `Documentos.pareceDeLaAplicacion`. Batería completa en verde. **Toca `apps-script/gestor-correos.gs`: Francisco tiene que volver a pegar el fichero entero en script.google.com** (ya lo tenía pendiente por la fila 18; con pegarlo una vez basta para las dos). **Subido con pull request** (fmargon780/gestor-asuntos-ies#15), pendiente de que la batería en GitHub Actions se ponga en verde para fusionarlo solo (permiso permanente de Francisco); versión `App.VERSION`: `17-sep-2026 · 09:20`, se publicará al fusionar. |
| 22 | `docs/SEPARAR-Y-UNIR-PDF.md` | HECHA | Acordado con Francisco el 17-sep-2026; es el punto 4 de `docs/PROXIMOS-ASUNTOS.md`, y con él la lista del 14-sep queda entera. Tres acciones nuevas (botones sueltos, no un desplegable) en cada fila de PDF, en la ficha del asunto (`js/ficha-asunto.js`) y en las tarjetas de Por clasificar (`js/documentos-sueltos.js`): Separar (rejilla de miniaturas con pdf.js y una tijera entre cada dos páginas; el resumen de arriba se recalcula solo), Unir (los demás PDF del mismo sitio, con casilla, cuenta de páginas y flechas para ordenar) y Sacar páginas (la misma rejilla, con casillas; el original no se toca ni va a la papelera). `js/pdf-herramientas.js` (`window.PdfHerramientas`) hace el trabajo de bytes con **pdf-lib** (`js/lib/pdf-lib.min.js`, copiada como pdf.js y cargada solo cuando hace falta: un PDF no se puede partir a mano como el `.docx` de la fila 17); `js/pdf-separar-unir.js` (`window.PdfSepararUnir`) pinta los cuadros. En un asunto, Separar pasa por el cuadro de poner nombre de siempre, una vez por trozo en secuencia (si se pulsa "Dejarlo" a mitad, lo ya guardado se queda); en Por clasificar, nombre automático sin preguntar nada. `Carpetas.escribirBytes` nueva. Nunca se pisa un fichero (se avisa y no se toca nada). Los originales de Separar y Unir van a la papelera; Sacar páginas no toca el original. Pruebas nuevas `pruebas/separar-unir.mjs` (sin navegador, con un PDF de prueba montado con la propia pdf-lib) y `pruebas/separar-unir-navegador.mjs` (navegador de verdad); de paso se ha arreglado un fallo del disco de mentira compartido (`pruebas/navegador.mjs`) que corrompía los bytes de un fichero binario de verdad al escribirlo (leía con `.text()` en vez de `.arrayBuffer()`). Batería completa en verde. No toca el script de Apps Script. Alcance decidido aquí: no se añaden estos botones dentro del cuadro "Gestionar documentos" (ya tiene cuatro botones por fila), y el botón Separar siempre sale (con un PDF de una sola página, el propio cuadro avisa de que no hay dónde cortar, en vez de leer el número de páginas de cada PDF solo para decidir si el botón se enseña). **Subido con pull request** (fmargon780/gestor-asuntos-ies#16), pendiente de que la batería en GitHub Actions se ponga en verde para fusionarlo solo (permiso permanente de Francisco); versión `App.VERSION`: `17-sep-2026 · 12:10`, se publicará al fusionar. |
| 23 | `docs/REFRESCO-DE-PANTALLA.md` | HECHA | **La primera de todas.** La lógica de repintado ya estaba bien (comprobado con el disco de mentira: las tres acciones refrescaban solas); el fallo real es que nada avisaba de que se estaba guardando, y con la carpeta de verdad en Dropbox el guardado tarda lo bastante para parecer que no ha hecho nada. `U.mientrasGuarda(control, fn)` nuevo (`js/util.js`): apaga el control ("Guardando…" en un botón) mientras se guarda. Puesto en estado, vía, plazo, archivar/reabrir y en marcar/cambiar de rama/responsable/fecha/notas/documentos de un hito. Prueba nueva `pruebas/refresco.mjs`. Batería completa en verde. Fusionado con pull request (versión `App.VERSION`: `17-sep-2026 · 04:23`); esta sesión no ha podido comprobarlo con `curl` (su red no llega a la web publicada, igual que la fila 10). Detalle en `docs/HISTORIA.md`. |
| 24 | `docs/NO-PISARSE-EN-UN-ASUNTO.md` | HECHA | **La tercera, después de la 25.** Señal de presencia en `_GESTOR/presencia.json` (módulo nuevo `js/presencia.js`), a propósito fuera de los doce ficheros protegidos: se escribe y relee directo con `Carpetas`, nunca con `Copias.guardar`, y no entra en copias, papelera ni fusión de conflictos. Al abrir la ficha de un asunto libre se anuncia la propia señal (se renueva cada 30 s, caduca a los 3 min); si otro ya está dentro, se entra en modo consulta: aviso arriba y "Tomar el mando", con todos los controles que modifican apagados (recorriendo la ficha entera, sin tocar `js/hitos-panel-lista.js`, `js/correo.js` ni `js/plantillas-documento.js`) salvo una lista blanca de solo lectura. Marca en la tarjeta de la lista (envolviendo `App.tarjetaAsunto`, sin tocar `js/asuntos-lista.js`). Descartada, por ahora, una base de datos en internet para que el aviso fuera instantáneo. Prueba nueva `pruebas/presencia.mjs` (la "otra persona" se simula como en `pruebas/conflictos.mjs`). Batería completa en verde. Detalle en `docs/HISTORIA.md`. |
| 25 | `docs/POR-CLASIFICAR-DOCUMENTO-A-LA-VISTA.md` | HECHA | **La segunda, después de la 23.** El documento que se ve en el panel de la derecha queda marcado en su tarjeta de la lista (`.tarjeta-abierta`), con desplazamiento solo hasta ella; la cabecera del panel enseña el nombre completo, cortado por el medio si no cabe; y los tres botones de la tarjeta (Crear asunto con él, Meter en un asunto, Borrar) salen también dentro del panel, reutilizando la misma tarjeta (`App.accionesDeSuelto`), sin duplicar la lógica. `js/visor.js` gana un marcador y un hueco de acciones, los dos opcionales: `js/ficha-asunto.js` y `js/unir-asuntos.js` siguen igual. Prueba nueva `pruebas/documento-a-la-vista.mjs`. Batería completa en verde. Fusionado con pull request junto a la fila 23, mismo pull request #10. Detalle en `docs/HISTORIA.md`. |
| 26 | `docs/HITOS-SON-LA-GUIA.md` | HECHA | Versión 17-sep-2026 · 16:40. Los hitos se crean solos, sin botón ni preguntar nada, al abrir la ficha de un asunto abierto cuyo tipo tiene guía y todavía no los tiene (respetando `pasosHechos`/`pasosElegidos` si el asunto ya existía desde antes). Cerrojo por clave (`creandoDesdeGuia`) contra la doble creación del propio repintado, y comprobación contra `App.E.registro` para no recrearlos si el asunto se archiva en ese mismo instante (se encontró y se arregló probándolo: el escenario 9 de `pruebas/hitos.mjs` los recreaba). La guía ya no se pinta como texto con casillas en la ficha: el bloque se llama "Hitos" y conserva el `<p class="nota" id="ficha-guia-nota">` de escribir o cambiar la guía. Desaparecen el botón "Crear los hitos de la guía" y `#hitos-entrada`; sale "+ Añadir el primer hito" si el tipo no tiene guía. `js/ficha-asunto.js` se parte: los documentos de la carpeta pasan a `js/ficha-documentos.js` nuevo. `pruebas/guias.mjs` y `pruebas/opciones.mjs` reescritas contra los hitos (ya no contra `.paso-casilla`/`.guia-opcion`/`.guia-rama`); `pruebas/hitos.mjs` con su escenario 2 ajustado (ya no hay botón) y una prueba nueva de que abrir la misma ficha dos veces no duplica los hitos. Batería completa en verde, una sola pasada al final. Subido directo a `main`, sin pull request (permiso de esta misma fila). Detalle en `docs/HISTORIA.md`. |
| 27 | `docs/CORREOS-DENTRO-DE-POR-CLASIFICAR.md` | HECHA | Versión 17-sep-2026 · 18:20. La bandeja de correos ya no es un bloque suelto siempre desplegado debajo de las tres tarjetas: vive dentro de "Por clasificar" (`#zona-clasificar`), detrás de una barra plegable "Correos sin clasificar (N)" encima de `#lista-sueltos`, que se despliega y se pliega al pulsarla y **siempre arranca plegada** al entrar en la vista (sin memoria en `localStorage`, sin `<details>`: el mismo patrón que el plegado de Filtros). "Mirar ahora" se va dentro del bloque desplegado; desaparece la cabecera "Correos por convertir en asunto", que sustituye la barra. La lectura sigue corriendo con el bloque plegado, y el número de la barra se actualiza aunque no se esté mirando esa vista. La caja de envíos `#bandeja-envios` se queda donde estaba, fijada explícitamente a `paneles.nextSibling` (se ancló ahí porque su antiguo anclaje a `$('bandeja-correos')` habría roto al dejar esa caja de ser hermana de `.paneles`). `js/bandeja-correos.js` baja de 1.478 líneas: la parte de pantalla pasa a `js/bandeja-pantalla.js` nuevo, hablándose por `window.Bandeja` (ampliado, sin renombrar ni quitar nada de lo que ya usaban `js/bandeja-enlace.js` y `js/correo-adjuntos.js`). `pruebas/correos.mjs` y `pruebas/correo-dos-buzones.mjs` ajustadas (su `mirarLaBandeja()` entra en la vista y despliega la barra), con una prueba nueva en `pruebas/correos.mjs` de que arranca plegada con el número al día y se vuelve a plegar sola al reentrar; `pruebas/envios.mjs` y `pruebas/refresco.mjs` revisadas, sin cambios. Batería completa en verde. Subido directo a `main`, sin pull request (permiso de esta misma fila). Detalle en `docs/HISTORIA.md`. |
| 28 | `docs/LO-PIDE.md` | HECHA | Acordado con Francisco el 17-sep-2026. Apartado "Lo pide" en cada asunto: clave opcional `loPide` en la ficha (nombre, categoría, relación, correo, teléfono, vía, fecha, quien lo apuntó), escrita con `App.anotar`, `null` para vaciarla. Módulo nuevo `js/lo-pide.js` (`window.LoPide`): `opciones`/`controles` (con clases, nunca con id, para poder montarse a la vez en "Nuevo asunto" y en el cuadro de la ficha), `texto`/`correoDe`, `elegirDestinatarios` (pura, para probar sin cargar el cuadro de Correo) y `datosDeTutor` (sacada de `js/plantillas.js`, que ahora la llama en vez de tener su propia copia). Grupo "Lo pide (opcional)" en `#bloque-detalles` de `js/asuntos-nuevo.js`. En `js/ficha-asunto.js`: fila "Lo pide" bajo "Vía de comunicación", marca `.marca-lopide` en la cabecera, y botón "Lo pide" (con "Quitar el dato") solo en asuntos abiertos, apagado en modo consulta pero fuera de `esControlDeSoloLectura`. En `js/correo.js`: con el correo de quien lo pide conocido, se marca esa casilla sola (o va a "Otro correo" si no está en la lista), línea gris "Lo pidió..." encima de "Para", y `aQuien` para Séneca. Cuatro huecos nuevos en `js/plantillas.js` (`{quienlopide}`, `{quienlopiderelacion}`, `{quienlopidevia}`, `{quienlopidefecha}`). El parentesco real (padre, madre, abuela) queda fuera, a propósito: el RegAlum no lo trae; la relación se enseña como "Tutor legal 1/2". Prueba nueva `pruebas/lo-pide.mjs`, sin navegador (jsdom), con los siete escenarios del encargo. Batería completa en verde. No toca `js/conflictos.js`, `js/asuntos-editar.js`, `js/papelera.js`, `js/asuntos-lista.js` ni `apps-script/gestor-correos.gs`. **Fusionado con pull request** (fmargon780/gestor-asuntos-ies#19), batería en verde (un fallo de `registro.mjs` en la primera pasada de CI era intermitente, sin foco: en el reintento pasó limpio). Versión `App.VERSION`: `17-sep-2026 · 09:43`. Esta sesión no ha podido comprobarlo con `curl` ni con el conector de Vercel (su red no llega a la web publicada, igual que las filas 10 y 23; el conector de Vercel da 403 sobre este proyecto, ya descartado en `docs/CONTEXTO-CORTO.md`): falta confirmar en el navegador que se ve `App.VERSION` `17-sep-2026 · 09:43` o más nueva, y que el límite diario de despliegues de Vercel (superado hoy) no ha retrasado la publicación más de la cuenta. |
| 32 | `docs/ARCHIVAR-CARPETA-YA-EXISTE.md` | HECHA | **Era la primera de la cola**: acordado con Francisco el 17-sep-2026, con un asunto suyo atascado. Al archivar, `Carpetas.trasladar` dejaba la carpeta de destino a medias si la copia fallaba (Dropbox sincronizando, fichero bloqueado), y a partir de ahí todo intento devolvía "Ya hay una carpeta llamada ... en el destino" y el asunto no se podía archivar nunca más. Ahora `trasladar` limpia el destino a medias si algo falla (`try/catch` alrededor de crear, copiar y comprobar), y `Carpetas.fusionarEn` (nueva) junta las dos carpetas sin perder nada cuando el destino ya existe de verdad (mismo nombre y mismo tamaño: no se duplica; mismo nombre y distinto tamaño: se guarda con un "(2)" detrás), con comprobación final antes de quitar el origen (`removeEntry` directo, sin pasar por `js/papelera.js`: viviría en el sentido contrario). Aviso en el propio cuadro de confirmación de `App.cerrarAsunto`/`App.reabrirAsunto`, mismo botón, sin preguntar nada más. `App.cerrarAsunto`/`App.reabrirAsunto` salen de `js/documentos-sueltos.js` (pasaba de 400 líneas) a `js/asuntos-archivar.js` nuevo, cargado antes de `js/relacionados.js` y `js/hitos-archivo.js`, que los envuelven. Prueba nueva `pruebas/archivar-fusion.mjs`, sin navegador, con los siete escenarios del encargo (comprobado que falla sin el arreglo). Batería completa en verde. **Fusionado con pull request** (fmargon780/gestor-asuntos-ies#21). Versión `App.VERSION`: `17-sep-2026 · 10:57`. Esta sesión no ha podido comprobarlo con `curl` (su red no llega a la web publicada, igual que las filas 10, 23 y 28): falta confirmar en el navegador. |
| 29 | `docs/DNI-DEL-PERSONAL.md` | HECHA | Acordado con Francisco el 17-sep-2026. El DNI del personal no se veía: estaba escondido en "Ver los demás datos del fichero". `Datos.destacadosPersona` (`js/datos.js`) mete una fila `DNI` la primera de todas, por encima de `Puesto`, cuando `persona.documento` tenga algo (Séneca o alta a mano), y ya no se repite abajo. `App.piePersona` (`js/asuntos-nuevo.js`) pasa de enseñar el documento a secas, en medio de la línea de debajo del nombre, a un `DNI <documento>` etiquetado al final. Sin aviso por edad: eso es solo del alumnado, `js/dni.js` no se toca. Prueba nueva `pruebas/dni-personal.mjs`, sin navegador, más el ajuste de índices que le hacía falta a `pruebas/logica.mjs`. Batería completa en verde. **Fusionado con pull request** (fmargon780/gestor-asuntos-ies#20). Versión `App.VERSION`: `17-sep-2026 · 10:28`. |
| 30 | `docs/QUEDARSE-EN-EL-ASUNTO.md` | HECHA | Acordado con Francisco el 17-sep-2026. La causa: `App.verAbiertos` relee la carpeta entera y crea asuntos nuevos cada vez que se llama, y se llama sola desde `App.mirarLaCarpeta` (cada `App.SEGUNDOS_ENTRE_MIRADAS`); el asunto de la ficha se quedaba con un objeto viejo y su lista de documentos no se enteraba de lo nuevo. `js/ficha-asunto.js` envuelve `App.verAbiertos` para reenganchar sola el asunto de la ficha abierta (por su nombre, en la lista fresca) y repintarla, con `App.fichaAbierta()` (comprueba el DOM, no solo una variable que se queda puesta al cambiar de pestaña) y `App.reengancharFicha()` (reengancha, o vuelve a la lista con un aviso si el asunto ya no está). `anadirTipo` pasa a apoyarse en el mismo mecanismo, sin repetirlo. Vale para las siete formas de guardar un documento sin que ninguna tenga que saber de la ficha; Editar, Archivar/Reabrir y Borrar siguen siendo los únicos que de verdad vuelven a la lista. Prueba nueva `pruebas/quedarse-en-el-asunto.mjs`, en navegador de verdad, con los cuatro escenarios del encargo (comprobado que falla sin el arreglo). Batería completa en verde. **Fusionado con pull request** (fmargon780/gestor-asuntos-ies#22). Versión `App.VERSION`: `17-sep-2026 · 11:25`. |
| 31 | `docs/APUNTAR-DOCUMENTO-A-HITO.md` | HECHA | Acordado con Francisco el 17-sep-2026, después de crear sus primeros hitos y no encontrar la forma de asociarles un documento. Es la mitad que falta de la fila 15: el modelo ya guardaba los documentos apuntados (`Hitos.anadirDocumento`/`quitarDocumento`), la lista se pintaba y el ✕ funcionaba, pero no había forma de apuntar uno. Botón "Apuntar un documento" en el cuerpo desplegado de cada hito abierto, que abre `js/hitos-documentos.js` (nuevo, `window.HitosDocumentos`): cuadro con casillas sobre `Carpetas.ficheros(a.handle)`, al aceptar llama a `anadirDocumento`/`quitarDocumento` y pide el repintado. El bloque de documentos se pinta siempre que el asunto esté abierto, aunque esté vacío; el nombre apuntado es pulsable y abre el documento en el panel de la derecha; un apuntado que ya no está en la carpeta sale en gris con "(ya no está)". Apuntar sigue siendo solo señalar: no se copia, no se mueve, no hay subcarpetas. Dos trampas de verdad, encontradas probándolo en un navegador (no solo leyendo el código): saber qué apuntado ya no está se lee **una sola vez por repintado**, en `js/hitos-panel.js`, antes de tocar el DOM (corregirlo después, a mano, hacía que el `MutationObserver` de `js/hitos-panel.js` se repintara sin parar); y el botón "ya no está" **nunca lleva el atributo `disabled`**, porque `js/ficha-asunto.js` (`aplicarModoConsulta`) reactiva solo, sin preguntar por qué, todo lo apagado dentro de `#ficha-asunto-cuerpo` en cuanto no hay nadie en modo consulta — basta la clase `hito-doc-falta`. Prueba nueva (escenario 6) en `pruebas/hitos.mjs`, en navegador de verdad, con los cuatro pasos del encargo. Batería completa en verde. **Fusionado con pull request** (fmargon780/gestor-asuntos-ies#23). Versión `App.VERSION`: `17-sep-2026 · 12:06`. Esta sesión no ha podido comprobarlo con `curl` (su red no llega a la web publicada, igual que las filas 10, 23, 28 y 32): falta confirmar en el navegador. |
| 33 | `docs/TABLON-NO-SE-BORRA.md` | HECHA | **Fallo urgente.** El repintado cada 10 segundos de la fila 24 (`js/presencia.js`) llamaba a `App.pintarAbiertos()` siempre, hubiera cambiado algo o no; como el tablón vive dentro de `#pantalla-abiertos`, se llevaba por delante `#tablon-texto` a medio escribir. `Presencia.huella()` nueva (claves y usuarios de la caché, en texto) deja que el intervalo solo repinte si de verdad cambia quién está dentro de algún asunto, con una segunda barrera que lo frena entero mientras el foco esté en cualquier `input`/`textarea`/`select`/`contenteditable`. En `js/tablon.js`, lo escrito en la nota nueva vive también en variables del módulo (`borrador`, `borradorFecha`, al día con `input`/`change`), no solo en el DOM, y `pintar()` devuelve el foco y el cursor (`selectionStart`/`selectionEnd`) al campo nuevo o al de una nota en edición. Cambios quirúrgicos, sin tocar nada más. Prueba nueva `pruebas/tablon-no-se-borra.mjs`, comprobado que falla sin el arreglo. Batería completa en verde. Subido directo a `main`, sin pull request. Versión publicada `App.VERSION`: `17-sep-2026 · 13:55`. |
| 34 | `docs/NOTAS-DEL-ASUNTO-NO-SE-BORRAN.md` | HECHA | **Fallo urgente, el mismo que la 33 pero donde Francisco lo sufría de verdad.** La ficha se repintaba entera cada vez que se reenganchaba (`App.mirarLaCarpeta` → `App.verAbiertos` → `App.reengancharFicha`, fila 30), hubiera cambiado algo o no, y rehacía con `innerHTML` el `<textarea id="ficha-nota-texto">` de la nota del asunto y el `<textarea class="hito-nota-texto">` de la nota de un hito. Lo que lo disparaba no era el reloj: bastaba con que el compañero dejara un papel suelto en "Por clasificar" para que la ficha abierta se rehiciera entera. `U.conservandoLoEscrito(raiz, fn, clavePara)` nueva en `js/util.js` (apunta valor, foco y cursor de cada `textarea`/`input` de escribir y se los devuelve a los que vuelvan a salir **vacíos**; la identidad es el `id`, un `data-clave`, o la que pase quien llama). La usan `pintar()` y `pintarNotas()` de `js/ficha-asunto.js` y el repintado entero de `js/hitos-panel.js` (dentro del `try`, con el observador desconectado, y volviendo a desplegar antes el hito que tuviera algo a medias, para no devolver el foco a un campo escondido). Y la otra mitad: **la ficha ya no se repinta si no ha cambiado nada**, comparando una huella de texto del asunto, partida en dos (ficha e hitos) para que un cambio solo de hitos le pida un repintado al panel de hitos en vez de rehacer la ficha entera. Trampa encontrada probándolo, que no estaba en el encargo: con la pantalla quieta, los botones ya pintados se quedaban con el objeto viejo del asunto y "Archivar el asunto" volvía a preguntar "¿Dónde va esta carpeta?" con la categoría ya puesta; se arregla metiéndole los datos frescos DENTRO al objeto que la ficha ya tiene, que es el que se queda en `App.E.listaAbiertos` (un asunto, un solo objeto en toda la aplicación). Prueba nueva `pruebas/notas-asunto-no-se-borran.mjs`, en navegador de verdad, comprobado que falla sin el arreglo (10 fallos). No ha hecho falta tocar `js/hitos-panel-lista.js` ni `pruebas/hitos.mjs`. Batería completa en verde (933 comprobaciones, 42 ficheros), comprobada también contra lo que quedó en `main`. **De paso se ha recuperado `docs/CONTEXTO.md`**, que estaba en `main` con la palabra `PLACEHOLDER_WILL_REPLACE` y nada más: restaurado del todo (17-sep-2026, ver la nota "Arreglado" al final de esta cola). Subido directo a `main`, sin pull request. Versión publicada `App.VERSION`: `17-sep-2026 · 15:47`. |
| 35 | `docs/HUECOS-INSERTAR.md` | HECHA | Acordado con Francisco el 17-sep-2026. El editor de una plantilla de correo (Ajustes › Plantillas de correo, `js/plantillas-ajustes.js`) pintaba un botón por cada hueco (más de treinta): se sustituye por un solo botón "Insertar hueco" que abre un cuadro pequeño y flotante (no un segundo `U.preguntar`: el editor ya está usando el único que hay) con buscador, sin mayúsculas ni tildes, navegable con flechas y Enter, y que cierra sin insertar con Escape (con su propio `stopPropagation`). El hueco elegido entra donde estuviera el cursor; sin haber tocado el campo todavía, al final. Pieza nueva `js/huecos-buscador.js` (`window.HuecosBuscador`, `HuecosBuscador.montar({boton, campos, huecos})`), cargada antes de `js/plantillas-ajustes.js`. **El formulario de hoy solo tiene un campo con huecos** (`#pl-texto`): no existe ningún campo de "asunto del correo" con huecos como daba por hecho el encargo. `js/plantillas-documento.js` no se ha tocado: su lista de huecos (`#pd-huecos`) es una tabla de referencia con "Copiar", no el mismo muro. CSS nuevo en `css/ajustes.css`. Prueba nueva `pruebas/plantillas-huecos.mjs`, en navegador de verdad. Batería completa en verde. **Varias sesiones en paralelo trabajaron esta misma fila a la vez** (dos implementaciones equivalentes: una con `U.engancharInsertarHueco` en `js/util.js`, otra con el módulo `js/huecos-buscador.js` de arriba, que llegó primero a `main` en piezas sueltas a lo largo de varios commits — módulo, enganche, `index.html` y pruebas por separado). Al fusionar se ha dejado la de `js/huecos-buscador.js` (ya en `main`) y se ha quitado la ayuda `U.engancharInsertarHueco`, que se quedaba sin usar. Versión `App.VERSION`: `17-sep-2026 · 17:37`. |

| 36 | `docs/FILAS-QUE-NO-SE-ESTRUJAN.md` | HECHA | Acordado con Francisco el 17-sep-2026, a partir de una captura suya: con el panel de la derecha abierto, el nombre de un documento se quedaba en un carácter por renglón. `css/filas.css` nuevo (enlazado el último en `index.html`): el texto de una fila no baja de un ancho mínimo, y los botones bajan a una segunda línea. `U.menuDeAcciones` nueva en `js/util.js`, el menú de tres puntos compartido. En `js/ficha-documentos.js` a la vista solo el nombre y "Registrar"; Copiar, Separar, Unir, Sacar páginas y Borrar van al menú. En `js/documentos-sueltos.js` a la vista "Crear asunto con él" y "Meter en un asunto"; Abrir, Separar, Unir, Sacar páginas y Borrar van al menú. Aplicado también a `.relacionado-fila`, `.hito-linea` y `.fila-tipo`; `.rejilla-tipos` y `#lista-personas` ya envolvían bien. La barra azul (`js/barra.js`) se pliega sola al abrir el visor o el lector y vuelve al cerrarlo, y pierde el tope de 1360px que tenía en `css/barra.css` con la barra plegada. Prueba nueva `pruebas/filas-estrechas.mjs`; ajustadas `documentos-sueltos.mjs`, `documento-a-la-vista.mjs`, `papelera.mjs`, `separar-unir-navegador.mjs` y `tablon.mjs`, que daban por hecho que esos botones estaban siempre a la vista. Batería completa en verde. Detalle en `docs/HISTORIA.md`. Versión `App.VERSION`: `17-sep-2026 · 19:18`. |
| 37 | `docs/FICHA-DEL-ASUNTO-NUEVA.md` | HECHA (17-sep-2026) | Acordado con Francisco el 17-sep-2026, en la misma conversación que la fila 36; va después de ella. La ficha se recoloca: izquierda Hitos y Documentos (los documentos se mudan desde la derecha); derecha "Datos y contacto" (la primera), Otros asuntos, Relacionados, Datos del asunto y Notas (la última). Con el panel de la derecha abierto (`body.con-visor`/`body.con-lector`) o por debajo de 1000px, una sola columna, en ese mismo orden. "Datos y contacto" pasa a una sola línea (nombre · grupo o etiqueta · edad · un teléfono · DNI, cada uno con su copiar) con botón "Ver todo"; etiqueta ámbar `NO MATRICULADO AA-AA` (con "última matrícula" debajo) o azul `SOLICITANTE`, con los colores ya existentes de `css/estilos.css`. "Ver todo" (`U.preguntar`, ancha, un solo botón) enseña Identificación, Matrícula y grupo, Contacto del alumno, **Tutores legales agrupados por persona**, Otros datos de la familia y el volcado plegado de siempre; para personal/empresas/otros, una versión más corta. Dos funciones nuevas y puras en `js/datos.js`: `Datos.tutoresDe(alumno)` (agrupa lo que hoy `Datos.destacadosAlumno` vuelca columna a columna y mezclado, leyendo el número de tutor y la clase de dato del título de cada columna; una columna de familia sin número no se pierde, cae en `.otros` del propio array) y `Datos.resumenDeTercero(persona, categoria)` (los datos de la línea; no rompe `Datos.destacadosAlumno`, que sigue igual para Personas y `js/asuntos-nuevo.js`). Módulos nuevos `js/ficha-tercero.js` (`window.FichaTercero.pintarLinea(caja, a)`, igual que `FichaDocumentos.pintar`) y `css/ficha-tercero.css`, para no engordar `js/ficha-asunto.js` (que de hecho baja, de 1.065 a 968 líneas: se le quitan `pintarContacto`/`contactoPlegado`/`contactoSuelto` y el cuerpo de `pintarLasNotas`, sustituidos por envoltorios de una línea). Las notas del asunto pasan a ser una caja de escribir directa (`Notas.pintarEnFicha`, `js/notas.js`): sin botón "Añadir nota", se guarda sola con `U.mientrasGuarda` y un retardo de un segundo desde la última tecla (`Notas.sustituir` con una clave de sesión, `borradorAbierto`, para seguir metiendo texto en la MISMA nota mientras la ficha se repinta sola por debajo; `App.abrirFicha` llama a `Notas.olvidarBorrador()` para que la próxima ficha que se abra empiece una nota nueva). **Trampa encontrada probándolo, que no estaba en el encargo**: el guardado automático cambia `a.ficha` por dentro sin que nadie más se entere, y eso dejaba la huella de "algo ha cambiado" (fila 34) atrasada: el siguiente repintado en segundo plano (cada pocos segundos) se creía que algo había cambiado DE VERDAD y rehacía la ficha entera, llevándose por delante una nota de un hito que se estuviera escribiendo a la vez en otro campo (lo pilló `pruebas/notas-asunto-no-se-borran.mjs`, y de rebote también `pruebas/notas-no-se-borran.mjs`, un test casi igual de otra sesión en paralelo el mismo día). Se arregla pasando `apuntarHuella` (ya existía en `js/ficha-asunto.js`) como cuarto argumento de `Notas.pintarEnFicha`, para volver a apuntar la huella justo después de cada guardado automático. Sin botón "Escribirle" en las tarjetas de tutor: `js/correo.js` no expone ninguna función pública para abrir su cuadro con un destinatario puesto (su `abrirCuadro` es privado a su propio IIFE) y tocar ese fichero se salía de esta fila, tal y como preveía el propio encargo. Prueba nueva `pruebas/ficha-tercero.mjs` (jsdom, sin navegador) con los 8 escenarios del encargo; escenario nuevo en `pruebas/notas-asunto-no-se-borran.mjs` (orden de bloques y que la nota se guarda sola, sin perder lo escrito). Batería completa en verde (comprobado además que las 2 pruebas que fallaban a la primera pasada, por la trampa de la huella de arriba, quedan en verde tras el arreglo). **Al empezar esta fila, `docs/COLA.md` estaba corrupto en `origin/main`** (la misma trampa de la regla 14, en el mismo sitio que le pasó a la sesión de la fila 48; ver la nota de esa fila); el `git merge` de esta fila se resolvió quedándose con la versión buena. Subido con pull request (fmargon780/gestor-asuntos-ies#29). Versión `App.VERSION`: `17-sep-2026 · 20:05`. |
| 38 | `docs/LO-PIDE-NOMBRE-DEL-TUTOR.md` | HECHA (17-sep-2026) | Apuntada 17-sep-2026 por Francisco: en "Lo pide", las opciones de tutor legal salían con un número (su documento) en vez de su nombre. `LoPide.datosDeTutor` (`js/lo-pide.js`) cogía como nombre "la primera columna que no fuera teléfono ni correo", y en el RegAlum del centro esa primera columna es el documento del tutor, no su nombre. Cambio quirúrgico, solo en cómo se elige la columna del nombre: `RE_NO_ES_NOMBRE` nueva descarta además documento/dni/nif/nie/pasaporte, identificación/identificador, número/num/nº/código, parentesco/relación/sexo, fecha/nacimiento y domicilio/dirección/localidad/municipio/provincia/país/nacionalidad/postal. De las columnas que sobreviven, el nombre se arma por prioridad: Apellidos + Nombre si hay las dos (una coma, salvo que Apellidos ya traiga una), si no la que haya de las dos, si no la primera que quede (como antes). Red de seguridad: el valor final tiene que traer al menos una letra (`\p{L}`) o el nombre se queda vacío, para que un número suelto nunca vuelva a colarse. Si no hay nombre pero sí teléfono o correo, `LoPide.opciones` ya no descarta la opción: la sigue ofreciendo como "Tutor legal 1" (o 2) a secas, guardando `relacion: ''` para que la línea de la ficha no la repita entre paréntesis. Teléfono, correo, guardado y el resto de `LoPide` sin tocar; `js/plantillas.js` (`{tutor1}`/`{tutor2}`) se beneficia sin cambiar nada, porque sigue llamando a `LoPide.datosDeTutor`. Pruebas nuevas en `pruebas/lo-pide.mjs` (escenario "2b"), con los cuatro juegos de columnas de la sección "Pruebas" del encargo (nombre en columna propia, apellidos+nombre separados, solo la columna del número da nombre vacío, sin columnas del tutor 2 no aparece la opción) más el caso de solo teléfono/correo y la comprobación de que `{tutor1}` trae el nombre. Batería completa en verde, una sola pasada al final (49 ficheros de prueba). Subido con pull request (fmargon780/gestor-asuntos-ies#30), junto con la fila 40. Versión publicada `App.VERSION`: `17-sep-2026 · 20:32`. |
| 39 | `docs/AJUSTES-POR-TIPO.md` | HECHA (17-sep-2026) | Acordado con Francisco el 17-sep-2026. Ajustes pasa a tener tres pestañas arriba: **Tipos de asunto** (lo de siempre: pestañas de categoría, buscador cruzado, aviso en vivo, sin perder nada), **El centro** (estados, tipos de documento, campos propios, grupos de personas, responsables/no lectivos de hitos, ficheros de datos, cómo se abrevia cada grupo, y datos del centro y firma) y **Mantenimiento** (carpetas de este ordenador, bandeja de correo, aviso de RegAlum viejo, copias, papelera, conflictos de Dropbox, duplicados descartados y fichas sin carpeta: los ocho bloques que ya existían, movidos enteros sin tocarlos por dentro, solo cambiando a qué contenedor apuntan). Pulsar una tarjeta de tipo ya no abre un cuadro: abre su **pantalla entera**, a dos columnas (una sola por debajo de 1000px), con siete secciones siempre desplegadas, sin ningún `<details>`: Datos del tipo, Campos, Pasos del trámite, Plantillas de correo y de Séneca, Plantilla de documento de Word, Plazo y Se repite. Cada sección reutiliza el editor que ya existía, pintado dentro de su hueco: `js/campos.js` (el cuadro `App.pintarCuadroDeCampos` se convierte en `App.construirSeccionCampos`, con el "Aceptar" sustituido por un botón propio "Guardar campos", y aviso ámbar de en cuántos tipos más se usa un campo propio compartido, `Campos.tiposQueUsanPropio`), `js/guias-enganche.js` (sin tocar: ya exponía `GuiasDelCentro.escribir`/`pasosDe`, justo lo que hacía falta), `js/plantillas-ajustes.js` y `js/plantillas-documento.js` (nueva función `pintarDeTipo(contenedor, tipo)` en cada uno, filtrada por tipo, con el cuadro de alta preseleccionando el tipo; los campos de "Datos del centro y firma" pasan de HTML generado por JS a estático en `index.html`, y ese fichero solo los rellena y los guarda) y `js/recurrentes.js` (`filaDeRecurrente` sacada a función compartida, nueva `pintarEnContenedor(contenedor, tipo)` filtrada, `alta(tipoPreset, alGuardar)` con el tipo ya puesto). El menú de los tres puntos de la tarjeta se queda con Cambiar el nombre y Quitar; "Campos" desapareció de ahí. La pantalla del tipo se registra en `App.PANTALLAS` igual que "asunto" o "que-me-toca": el botón "← Volver" y la tecla Escape los pone solos `js/usabilidad.js`, sin escribir ni una línea de más, porque su cabecera tiene la misma forma (`<header class="cabecera"><h2>`) que las demás pantallas. Las tres pestañas y la pantalla del tipo suben el ancho a 1600px (`css/vista.css`). `js/ajustes.js` (52 KB) se parte en `js/ajustes.js` (marco: pestañas, lista de tipos, buscador cruzado, ~500 líneas), `js/ajustes-tipo.js` (nuevo), `js/ajustes-centro.js` (nuevo) y `js/ajustes-mantenimiento.js` (nuevo); también se tocan `index.html`, `css/ajustes.css` y, para retargetear su contenedor sin tocar su lógica, `js/hitos-ajustes.js`, `js/conflictos.js`, `js/fichas-huerfanas.js`, `js/frescura.js`, `js/bandeja-correos.js` y `js/unir-asuntos.js`. **Trampa encontrada probándolo en un navegador de verdad, que no estaba en el encargo**: al mover la sección Campos a la pantalla del tipo, se pinta con su contenido ANTES de colgarse del documento (la sección entera se construye y solo luego se añade a su columna), así que el `$` global (`document.getElementById`) no encontraba nada dentro; se soluciona sombreando `$` dentro de esa función por uno que busca con `cuerpo.querySelector`. Otra, de las mismas pruebas: al partir `js/ajustes.js` se perdió por descuido el `onclick` de `#btn-anadir-tipo` (se movieron los de estados/tipos de documento/campos propios/grupos a `js/ajustes-centro.js`, y ese se quedó sin sitio); lo pilló `pruebas/navegador.mjs` fallando en "el tipo nuevo se guarda en mayúsculas", y quedó devuelto a `js/ajustes.js`, que es donde sigue viviendo la pestaña de tipos. Pruebas: `pruebas/ajustes-agil.mjs` actualizada (las tres pestañas; el paso 8, antes "once bloques en una sola lista", ahora cuenta los de "Mantenimiento", que sigue siendo la pestaña más larga); nueva `pruebas/ajustes-por-tipo.mjs` (las tres pestañas, las siete secciones sin plegar, cambiar Plazo y Campos y que se guarden de verdad, volver sin perder categoría ni buscador, Escape). Doce ficheros de prueba más tocados por el cambio de sitio de los bloques de Ajustes, sin tocar su lógica: `pruebas/campos.mjs` (el flujo entero pasa de cuadro a pantalla del tipo), `pruebas/guias.mjs`, `pruebas/plantillas-huecos.mjs`, `pruebas/conflictos.mjs`, `pruebas/duplicados.mjs`, `pruebas/grupos-navegador.mjs`, `pruebas/huerfanas.mjs`, `pruebas/hitos.mjs`, `pruebas/navegador.mjs`, `pruebas/papelera.mjs`, `pruebas/correo-dos-buzones.mjs`, `pruebas/correos.mjs` y `pruebas/envios.mjs` (estas tres últimas, un solo `App.cambiarPestanaAjustes('mantenimiento')` antes de mirar la bandeja). Batería completa en verde, una sola pasada al final (50 ficheros de prueba). Subido a la rama `claude/kind-hawking-io2lou`, con el pull request #31 ya abierto para esta fila. Versión `App.VERSION`: `17-sep-2026 · 21:47`. |
| 40 | `docs/SALTAR-A-OTRO-ASUNTO.md` | HECHA (17-sep-2026) | Acordado con Francisco el 17-sep-2026. Va después de la fila 37. El bloque "Otros asuntos de este tercero" de la ficha solo pintaba texto muerto: ahora cada línea es un `<button>` que se pulsa y abre la ficha de ese asunto. Módulo nuevo `js/otros-del-tercero.js` (`window.OtrosDelTercero`), que se lleva tal cual `listaDeOtros`/`pintarOtrosDelTercero` de `js/ficha-asunto.js` (que ya pasaba de 960 líneas y baja a 934: no podía engordar más), mismo patrón que `js/relacionados.js` — la ficha solo cuelga el hueco `#ficha-otros`, con la categoría, el tercero (`nombreDelTercero`, que se queda en `js/ficha-asunto.js` porque también la usa "Lo pide") y el tipo ya calculados. Un asunto ABIERTO se busca en `App.E.listaAbiertos` (`App.abrirFicha(a, 'abierto')`, igual que `irAlCandidatoAbierto` de `js/duplicados.js`); uno del ARCHIVO **nunca dispara `App.verArchivo`** (recorre el ARCHIVO entero): se monta a mano con una sola lectura de carpeta, calcada de cómo `App.verArchivo` monta cada asunto en `js/archivo-personas.js` (`Duplicados.carpetaDelTercero`, exportada con una línea nueva). Todo en `try/catch`: si la carpeta ya no está, aviso de una línea y no se salta a ningún sitio roto. En la ficha a la que se salta sale arriba, junto a "Volver a la lista", el botón "← Volver a [asunto de partida]" (nombre recortado por CSS con `text-overflow: ellipsis`, entero en el `title`, para que la cabecera nunca crezca ni se parta en dos alturas): el módulo guarda `origen = { nombre, modo, categoria, tercero }` la primera vez que se salta y no lo sustituye hasta que se olvida, así que saltando A → B → C el botón en C sigue diciendo "Volver a A", nunca "Volver a B". El origen se olvida al pulsar el propio botón de vuelta, al pulsar "Volver a la lista" y al abrir cualquier ficha desde otro sitio (la lista, "Qué me toca", duplicados, Por clasificar): el módulo envuelve `App.abrirFicha` (la envoltura vive dentro del mismo cierre del módulo, no aparte, porque necesita ver directamente la bandera interna `saltandoDesdeAqui` que distingue sus propias llamadas). `esControlDeSoloLectura` deja encendido `#ficha-volver-al-origen` en modo consulta. Prueba nueva `pruebas/saltar-a-otro-asunto.mjs`, en navegador de verdad, con los 6 escenarios del encargo (líneas pulsables, salto a un abierto, botón de vuelta y que devuelve, cadena A→B→C sin perder el origen, "Volver a la lista" olvida el origen, y un asunto del ARCHIVO se abre en modo solo lectura sin llamar nunca a `App.verArchivo`, comprobado con un contador espía). Batería completa en verde, una sola pasada al final (49 ficheros de prueba). No toca `js/salir.js` ni el comportamiento de Escape. Versión publicada `App.VERSION`: `17-sep-2026 · 20:26`. |
| 41 | `docs/LEER-DOCUMENTOS-POR-CLASIFICAR.md` | HECHA (17-sep-2026) | Acordado con Francisco el 17-sep-2026. Va después de la fila 39 (`AJUSTES-POR-TIPO`), porque la casilla de palabras clave vive en la pantalla propia de cada tipo. Cada tarjeta de "Por clasificar" lee ahora el PDF con pdf.js y **propone** (nunca decide) tipo, fecha, registro de Séneca y tercero. `js/registro-lector.js` gana `textoDe(fichero, topePaginas)` (5 páginas por defecto, cadena vacía si no es PDF o si pdf.js falla; `textoHastaElSello` y `leerSello` sin tocar), con una comprobación nueva de cabecera real (`%PDF-`) antes de cargar pdf.js, para no invocarlo con un fichero que solo tiene la extensión `.pdf` sin serlo de verdad. Módulo nuevo y **puro** `js/lector-documentos.js` (`LectorDocumentos.analizar(texto, contexto)`, sin disco, pantalla ni pdf.js): registro con `RegistroLector.buscarEnTexto`; fecha del sello o, si no, la primera `dd/mm/aaaa`, `dd-mm-aaaa` o "17 de septiembre de 2026" del texto (nunca la de hoy); documentos de identidad (DNI y NIE con la letra comprobada por tabla, NIF de empresa sin comprobar el dígito de control, Nº de identificación escolar como tirada de 5 a 10 cifras) cotejados contra tres listas de terceros que monta quien llama; tipo por coincidencias de palabras enteras (sin tildes ni mayúsculas) de `palabrasClave` más el propio nombre del tipo, sin empate. Un documento de identidad que cuadra vale más que un nombre que cuadra (probado por nombre en las dos formas, directa e invertida, solo si no hay ningún documento que coincida); si cuadran dos terceros distintos, o empatan dos tipos, no se propone ninguno. `tipos.json` gana la clave `palabrasClave` (lista de textos), editable en una sección nueva "Palabras clave" de la pantalla del tipo (`js/ajustes-tipo-palabras-clave.js`, `PalabrasClaveTipo.pintarDeTipo`, enganchada con tres líneas en `js/ajustes-tipo.js`, mismo patrón que Plantillas/Se repite: no engorda ese fichero). En "Por clasificar", `js/documentos-sueltos-lector.js` (nuevo, envuelve `App.tarjetaSuelto` igual que `js/papelera.js`, sin tocar `js/documentos-sueltos.js`) añade, solo a los PDF, una línea gris bajo el nombre: "Leyendo el documento…" mientras lee, y al terminar lo encontrado separado por puntos (`26EM0368 · 10-sep-2026 · SOLICITUD · García Pérez, Ana`), con el botón "Aceptar" pegado a la línea (no dentro de `.acciones`, que ya tiene su lista fija de botones comprobada en `pruebas/documentos-sueltos.mjs`) cuando hay tipo y tercero claros. "Aceptar" llama a `App.crearAsuntoConPropuesta` (nueva en `js/asuntos-nuevo.js`, ~15 líneas: `App.E.pendiente` + `App.ir('nuevo')` + `App.elegirCategoria`/`elegirTipo`/`fijarTercero`, ya existentes, y el mismo `$('btn-crear').onclick()` de siempre) — el mismo camino que "Crear asunto con él" a mano, con su aviso de duplicado (`js/duplicados.js`) intacto; `js/campos.js` no se ha tocado, porque el relleno de campos propios ya ocurre solo al fijar el tercero. Los ficheros se leen de uno en uno con una cola en memoria, nunca en paralelo, solo mientras la pantalla "Por clasificar" está abierta (nunca al arrancar), cacheados por nombre de fichero: volver a la lista no vuelve a leer nada. Sin OCR y sin ningún servicio de inteligencia artificial por internet: lo que no se lee, o un documento que no sea PDF, se queda exactamente como hoy, sin línea ni botón. `js/ajustes-tipo.js` (429 líneas) y `js/asuntos-nuevo.js` (~895 líneas) ya pasaban de las 400 antes de esta fila (pantallas de un solo módulo, mismo criterio que se dejó para `js/ajustes.js` en la fila 39): el añadido de esta fila en cada uno es una veintena de líneas de enganche, sin repetir lógica, así que no se han partido. Pruebas nuevas: `pruebas/lector-documentos.mjs` (sin pdf.js ni navegador, los 6 escenarios de la sección 8 del encargo: sello completo, DNI de un alumno de la lista, dos alumnos distintos sin proponer ninguno, dos tipos empatados sin proponer tipo, DNI con la letra mal descartado, texto vacío sin romper nada). `pruebas/ajustes-por-tipo.mjs` ajustada: la pantalla del tipo pasa de siete a ocho secciones. Batería completa en verde, una sola pasada al final (51 ficheros de prueba). Versión publicada `App.VERSION`: `17-sep-2026 · 22:13`. |
| 42 | `docs/TERCEROS-NUEVOS-DESDE-EL-DOCUMENTO.md` | EN CURSO (17-sep-2026) | Acordado con Francisco el 17-sep-2026. Va después de la fila 41. Si el documento trae un DNI/NIE/NIF que no está en ninguna lista, sale un botón "Dar de alta" que abre el alta con los datos ya escritos; la aplicación nunca da de alta sola. Categoría nueva "alumnado pendiente" para los aspirantes a plaza, con Nº de identificación escolar opcional: sin él la carpeta va sin número, y al escribirlo se renombran solos los asuntos abiertos (los archivados no). Aviso en "Qué me toca" mientras queden aspirantes sin número. |
| 43 | `docs/REPASO-DE-LA-COLA-2026-09-17.md` | HECHA (17-sep-2026) | Papeleo de la cola: añadidas las filas 44 y 45 (dos instrucciones subidas a `main` el 17-sep-2026 sin fila propia). Arreglada la cabecera de `docs/ARCHIVAR-ATASCOS.md` (ya no dice "fila 33"). `docs/HISTORIA.md` restaurado del todo, 140.232 bytes comprobados. **Numeración reconciliada al fusionar `main`**: otra sesión, en paralelo, se había apuntado ya los números 41 y 42 para `LEER-DOCUMENTOS-POR-CLASIFICAR.md` y `TERCEROS-NUEVOS-DESDE-EL-DOCUMENTO.md` (las mismas dos que esta sesión había encontrado sin fila); como ya tenían fila con esos números, no hacía falta añadirlas otra vez, solo correr la numeración propia de esta fila de 41 a 43. No toca código ni publica versión nueva. |
| 44 | `docs/BUSCADOR-ARCHIVO-INDICE.md` | PENDIENTE | Se subió a `main` el 17-sep-2026 a las 17:34 sin fila propia. **No está hecha**: `js/archivo-indice.js` no existe. Índice guardado del ARCHIVO y búsqueda por palabras sueltas, en los nombres de los documentos, en el número de registro de Séneca y en la ficha del asunto. Es la primera de tres instrucciones sobre el buscador del ARCHIVO; las otras dos están sin diseñar. |
| 45 | `docs/ARCHIVAR-ATASCOS.md` | HECHA | Escrita el 17-sep-2026 pidiendo ser la fila 33, pero ese número se lo llevó `TABLON-NO-SE-BORRA.md` y la instrucción se quedó fuera de la cola hasta ahora. Comprobado antes de empezar que las filas 32, 33 y 34 no dejaban esto resuelto: `U.mensajeDeError` no existía, `contarFicheros`/`copiarDentro`/la fusión no se saltaban los temporales de sincronización, y ni `App.cerrarAsunto` ni `App.reabrirAsunto` reconocían una carpeta ya movida. `U.mensajeDeError(e)` nuevo en `js/util.js` (por `e.name`: `NotFoundError`, `NotAllowedError`, `NoModificationAllowedError`/`InvalidStateError`, `QuotaExceededError`, `AbortError` traducidos; cualquier otro, tal cual, porque los nuestros ya están en castellano), usado en el `catch` de las dos funciones de `js/asuntos-archivar.js`: nunca más un NotFoundError en inglés en pantalla. En `js/carpetas.js`, `contarFicheros`, `copiarDentro` y `fusionarDentro` se saltan ahora todo lo que `esCarpetaTemporalDeSincronizacion` reconozca (ya existía de la fila 32, ahora también para ficheros al copiar), y un `getFile()` que revienta con `NotFoundError` a mitad de copia se reintenta una vez tras esperar un segundo (`leerFicheroParaCopiar`, usada por `copiarDentro` y por la fusión); si el fichero sigue sin estar, el error dice su nombre, en castellano, y no se borra nada. En `js/asuntos-archivar.js`: `App.cerrarAsunto` mira primero si la carpeta sigue en Asuntos abiertos; si no, y ya está en `ARCHIVO/categoría/tercero`, pone la ficha al día sin copiar nada y avisa en verde; si no está en ningún sitio, avisa en ámbar (clase CSS nueva `.mensaje.ambar` en `css/estilos.css`, con `--ambar-linea`) pidiendo pulsar Recargar, sin ningún mensaje en inglés. `App.reabrirAsunto` hace lo mismo con `a.padre` (el manejador guardado al pintar la pantalla ARCHIVO, que puede estar viejo): si no sirve, recalcula con `Carpetas.bajar(App.E.archivo, [ficha.categoria, ficha.tercero], false)`; si tampoco está ahí pero ya está en Asuntos abiertos, se da por reabierto sin copiar nada; si no aparece por ningún lado, el mismo aviso ámbar. Prueba nueva `pruebas/archivar-atascos.mjs`, en navegador de verdad (reutiliza el disco de mentira de `pruebas/navegador.mjs`, como `pruebas/tablon-no-se-borra.mjs`), con los seis escenarios del encargo; comprobado con `git stash` de los cuatro ficheros de código (sin tocar la prueba) que falla sin el arreglo. Batería completa en verde (44 ficheros de prueba). No se han partido `js/carpetas.js` (478 líneas; ya estaba en 449 antes de esta fila) ni `js/util.js` (421 líneas): partirlos habría obligado a tocar además `pruebas/copias.mjs`, `pruebas/dni-personal.mjs`, `pruebas/logica.mjs` y `pruebas/plantillas-documento.mjs` (cargan `carpetas.js` sueltos, sin más scripts), fuera del alcance de este documento; queda anotado por si conviene una fila dedicada. Versión publicada `App.VERSION`: `17-sep-2026 · 18:44`. |
| 46 | `docs/CABECERA-QUE-SE-QUEDA.md` | PENDIENTE | Subida a `main` el 17-sep-2026 · 18:38 pidiendo ser la fila 43, pero ese número ya lo llevaba este mismo repaso de la cola (fila 43). **Va después de la 37 y de la 39** (recolocan pantallas que esta instrucción también toca; si alguna sigue PENDIENTE cuando le toque el turno, se salta y se vuelve cuando estén hechas). La cabecera de cada pantalla se queda pegada arriba y se encoge a una sola línea al bajar, con un único mecanismo compartido (`js/cabecera-fija.js`, `css/cabecera-fija.css`) para las siete pantallas, en vez de siete parches sueltos. |
| 47 | `docs/DESTINATARIOS-EN-SENECA.md` | PENDIENTE | Subida a `main` el 17-sep-2026 · 19:01 pidiendo también ser la fila 43, ya ocupada; otra sesión más volvió a subir directamente a `main` esa misma fila 43 con este mismo documento poco después (19:06), sin saber que ya estaba reconciliada aquí como fila 47, así que se ha descartado su fila duplicada al fusionar. Viene de la fila 21 (`GRUPOS-DE-PERSONAS`): lleva lo mismo a la mensajería de Séneca, que no usa correos sino usuarios IdEA. Módulo nuevo `js/idea.js` (lee el usuario IdEA de los CSV por el título de la columna, como `js/dni.js`; el personal ya los trae en su CSV, el alumnado todavía no — su importación será una fila aparte); el desplegable "Añadir un grupo" se lleva también al cuadro de Séneca, con una lista de destinatarios en chips, "Copiar la lista" y "Copiar el siguiente"; y un "ayudante" (marcador de navegador, `js/seneca-ayudante.js`) que pega uno a uno en el buscador de Séneca. No depende de ninguna otra fila pendiente. |
| 48 | `docs/NO-GASTAR-PUBLICACIONES.md` | HECHA (17-sep-2026) | Llegó a `main` por otro camino, sin fila propia, pidiendo ser la fila 44; ese número ya se lo había quedado `BUSCADOR-ARCHIVO-INDICE.md`, así que quedó reconciliada como fila 48, la última de la tabla pero **la primera del orden de trabajo**. Acordado con Francisco el 17-sep-2026, el día que Vercel agotó el cupo del plan gratuito (100 publicaciones al día, `api-deployments-free-per-day`) y a Francisco le llegaron decenas de correos de error. `vercel.json` gana `ignoreCommand`, sin tocar el bloque `headers` que ya tenía: se salta la publicación cuando la rama no es `main`, o cuando el cambio solo toca `docs/`, `pruebas/`, `.github/` o ficheros `.md`, mirando `VERCEL_GIT_PREVIOUS_SHA` (la última publicación buena) como referencia y publicando ante cualquier duda. **`ignoreCommand` no puede pasar de 256 caracteres**: la primera receta, puesta tal cual dentro del JSON, tenía 296 y Vercel la rechazó (aviso en el PR de la fila 36, #28); la receta entera vive en `scripts/vercel-ignore-build.sh` (nuevo, ejecutable) y `ignoreCommand` solo lo llama (`bash scripts/vercel-ignore-build.sh`). **Dos sesiones trabajaron esta fila en paralelo sin saberlo**: la que encontró el límite de 256 dejó el arreglo bueno en `main` (PR #28); una segunda sesión, que se había bajado `main` antes de que ese PR se fusionara, subió después su propia versión con la receta larga dentro del JSON (sin usar el script) sin volver a bajar `main` justo antes de subir (contra la regla 10), y sin querer devolvió `vercel.json` a los 296 caracteres que Vercel rechaza. Detectado y corregido en el momento: `ignoreCommand` vuelve a llamar al script, y la prueba redundante que trajo esa segunda sesión (`pruebas/no-gastar-publicaciones.mjs`, que no comprobaba el límite de 256 ni el script) se ha quitado, dejando solo `pruebas/vercel-ignore-command.mjs` (la de la primera sesión, más completa: comprueba también el límite de caracteres y que `ignoreCommand` llama al script). Batería completa en verde. Regla 13 nueva en esta cola (como máximo dos subidas por fila) y regla 14 (nunca `$(cat fichero)` como valor de `content` al subir un fichero: el servidor no lo ejecuta, lo sube tal cual — también pasó en esta misma fila, con `docs/COLA.md` quedándose en 35 bytes un momento). No toca `index.html`, `js/` ni `css/`: no hay versión nueva de `App.VERSION` que publicar. **La comprobación con `curl` de que Vercel salta lo que no debe y publica lo demás sigue pendiente para el 18-sep-2026** (hoy el cupo está a cero): la primera sesión de mañana tiene que empezar por ahí. Detalle en `docs/HISTORIA.md` y `docs/CONTEXTO.md`. |

**Orden de trabajo:** quedan pendientes las filas 42, 44, 47 y 46, en ese orden.
La fila 41 (`LEER-DOCUMENTOS-POR-CLASIFICAR`) ya está hecha, así que la 42 (que iba después de
ella) puede empezar ya. La 44 (`BUSCADOR-ARCHIVO-INDICE`) no tenía prisa (nadie la reclamaba). La 47
(`DESTINATARIOS-EN-SENECA`) no depende de ninguna otra y se deja cerca del final por no tener
prisa tampoco, pero antes que la 46 (`CABECERA-QUE-SE-QUEDA`), que sigue siendo la última por ir
después de la 37 y de la 39, las dos más tardías del resto de la lista (las dos ya están hechas; la
46 puede empezar en cuanto le toque turno). Las filas 1 a 41 están hechas; la 43 (el repaso de
la cola), la 45 (`ARCHIVAR-ATASCOS`) y la 48 (`NO-GASTAR-PUBLICACIONES`) también.

## Arreglado: `docs/CONTEXTO.md` (roto durante la fila 33)

17-sep-2026. `docs/CONTEXTO.md` se quedó en `main`, durante la fila 33, con solo la palabra
`PLACEHOLDER_WILL_REPLACE` (24 bytes). Varias sesiones en paralelo intentaron recuperarlo del
historial de git (commit `a29a7cf`) troceándolo en varias llamadas por su tamaño, y cada una dejó
sin querer solo una parte del fichero (`create_or_update_file`/escribirlo a mano reemplaza el
fichero entero, no añade). Quedó arreglado del todo subiéndolo en una sola llamada con el fichero
entero delante (1.817 líneas del original más las anotaciones de las filas 33, 34 y 35).
Comprobado con `git show origin/main:docs/CONTEXTO.md` después de subir: sin ningún `PLACEHOLDER`.
De ahí sale la regla 11 de esta cola.

`docs/HISTORIA.md`: se le han añadido las entradas de las filas 33, 34 y 35 que faltaban (nunca
llegó a romperse, solo le faltaban esas filas).

**Nota sobre esta misma cola** (17-sep-2026): mientras se arreglaba esto, al menos tres sesiones
en paralelo (en la nube) han estado tocando `docs/COLA.md` a la vez, y más de una subida ha
pisado sin querer el arreglo de otra (la fila 35 volvió a `PENDIENTE`, y esta misma nota volvió a
su versión rota, más de una vez) al subir el fichero entero sin haber vuelto a bajar `main`
justo antes. Si esto se repite, hace falta que Francisco lance las sesiones de una en una, no en
paralelo, mientras la cola esté tan activa.

## Arreglado: `docs/HISTORIA.md` (se quedó a medias el 17-sep-2026 por la tarde)

Una sesión que intentaba añadirle a `docs/HISTORIA.md` una entrada de la fila 33 no pudo escribir
de una sola vez un fichero de más de unos 45-50 KB (regla 12 de esta cola): la llamada que sube el
contenido se corta sola, sin ningún error, y deja el fichero con solo el primer trozo. Pasó dos
veces seguidas: la primera dejó el fichero en 44 KB, la segunda en 48.766 bytes, muy lejos de sus
138.402 bytes de verdad (658 líneas en vez de las que le tocaban).

Se ha arreglado restaurando la versión completa que ya traía esta rama (con las entradas de las
filas 33, 34 y 35 incluidas: al contrario de lo que decía una nota anterior aquí, sí merecía la
pena contarlas, y ya estaban escritas desde antes de este percance). Comprobado con `wc -l`
después de fusionar: 2.073 líneas, sin quedarse corto. `docs/CONTEXTO.md` no se vio afectado por
este percance concreto: sigue entero, como dice la nota de arriba. Con la entrada de la fila 45
añadida encima (17-sep-2026, fusión de la fila 43 de esta cola), el diario queda en 2.123 líneas.

## Lo que vendrá después

Los nueve asuntos que Francisco dejó apuntados el 14-sep-2026 en `docs/PROXIMOS-ASUNTOS.md` están
ya todos hablados y metidos en esta cola: el 1 es la fila 19, el 2 la 17, el 3 la 21, el 4 la 22,
el 5 la 11, el 6 la 14, el 7 la 13, el 8 la 12 y el 9 la 20. Esa lista queda cerrada.

De la fila 21 se quedaron fuera, por falta de datos y no por falta de código: los departamentos del
personal, las tutorías y los equipos educativos. `personal.csv` no guarda nada de eso. Hay que
hablar con Francisco qué se puede sacar de Séneca antes de diseñar nada.

De la fila 28 se queda fuera, por la misma razón: el parentesco de verdad (padre, madre, abuela).
El RegAlum no trae esa columna, así que la relación se enseña como "Tutor legal 1" y "Tutor legal
2". Si algún día Séneca deja sacarla, se revisa.

De la fila 34 se queda fuera, a sabiendas: si la ficha entera se repinta de verdad (llega un
documento a la carpeta del asunto) mientras se escribe una nota **de hito**, esa nota sí se
pierde. El panel de hitos se vuelve a pintar un instante después, de forma asíncrona, y para
entonces la ayuda ya no tiene dónde devolverla. La nota del asunto sí sobrevive a ese caso.
Arreglarlo pedía memoria propia del panel de hitos entre repintados, con riesgo de resucitar texto
de otro asunto, y el caso es raro desde que la ficha casi no se repinta.

Guardado por si algún día se replantea (17-sep-2026): una base de datos pequeña en internet para
que el aviso de la fila 24 sea instantáneo en vez de tardar lo que tarde Dropbox. Descartada ahora
por la configuración y por depender de la conexión. Si se hace, solo viajarían el identificador
del asunto y el nombre de quien lo abre, nunca el nombre de la carpeta ni dato alguno de alumnado
o personal, y con servidor en la Unión Europea.

Cuando los hitos (fila 15) estén en uso, queda por hablar: si el estado del asunto desaparece y lo
sustituye el hito en curso, y si un hito puede apuntar a su plantilla de correo (fila 14) o a su
plantilla de documento (fila 17). Esto último Francisco ya lo dio por hecho el 16-sep-2026: las
plantillas acabarán colgando también de los tipos de hito.

## Nota sobre "sube directamente a main"

Algunas instrucciones (`docs/HITOS.md` entre ellas) piden subir a `main` sin pull request. Esta
sesión concreta de Claude Code (la de "en la nube", disparada desde GitHub) tiene forzado lo
contrario: trabajar en una rama propia y abrir un pull request, sin permiso para tocar `main`
directamente. Mientras se lance así, las filas de esta cola se suben con pull request. Si se
quiere volver a "directo a main", hay que lanzar la cola desde una sesión de Claude Code normal
(terminal u ordenador), no desde la nube.

**Permiso permanente de Francisco (16-sep-2026): fusionar el pull request lo hace Claude Code
solo**, sin esperar a que Francisco lo haga a mano. Antes de fusionar: la batería de pruebas
(`npm test`) tiene que estar en verde, el PR sin conflictos con `main` (`mergeable_state: clean`)
y sin ningún comentario de revisión pendiente de responder. Fusionado eso, Vercel publica solo:
comprobar lo publicado con `curl` sigue haciendo falta después, no antes.
